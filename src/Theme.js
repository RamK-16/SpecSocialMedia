import { createTheme } from "@mui/material";

const theme = createTheme({
  palette:{
    primary:{
      main: "#678",
      light: "skyblue"
    },
    secondary:{
      main: "#888"
    },
    otherColor:{
      main: "#999"
    }
  }
});
export default theme;
